/* ═══════════════════════════════════════════════════════════════════════════
   분석 화면 안의 "담백한" 칸들이 함께 쓰는 색 — **무채색 한 벌**.

   ★ 이 화면은 98 문법(회색 면 · 네 변 베벨 · 검은 1px 선)으로 지었지만, **흐름도와 월간
     지표 두 칸만** 흰 바탕에 담백하게 간다 (사용자 요청). 두 칸 다 숫자를 훑는 곳이라,
     칸마다 입체 테두리를 두르면 내용보다 테두리가 먼저 보인다.
   ★ 색을 **전부 걷어냈다** (사용자 지적 — 오히려 화려하다). 파랑·초록·빨강이 뜻을 나르던
     자리를 **글자 크기와 굵기**가 대신한다. 색이 여섯이면 여섯 다 중요해 보이는데, 회색
     한 벌에서는 굵은 것 하나만 눈에 든다 — 강조가 강조로 작동한다.
   ⚠️ 그래서 **뜻을 색에 싣지 않는다.** "쌓임 / 빠짐", "+ / −" 처럼 글자로 적는다. 색이
      없어져서가 아니라, 원래 색만으로 말하면 색각 이상이 있는 사람에게는 같은 회색이었다.
      지금은 모두에게 같은 화면이다.
   ⚠️ 색을 **여기 한 곳에서만** 정한다. 두 파일이 각자 `#1B1F24` 를 적어 두면 한쪽만 손댔을
      때 두 칸이 조금씩 다른 회색을 쓰게 되는데, 회색끼리라 눈으로는 한참 못 알아챈다.
   ⚠️ 창틀(`Panel`)은 98 그대로 둔다. 창 **안쪽**만 담백한 것이지, 창까지 달라지면 그 칸만
      다른 프로그램에서 떠 온 것처럼 보인다.
   ═══════════════════════════════════════════════════════════════════════════ */

/** 본문 글자 · 가장 진한 것. 강조는 이 색에 **굵기와 크기**를 더해서 만든다 */
export const INK = "#14181C";
/** 라벨·부연 — 본문보다 한 단계 물러난다 */
export const MUTED = "#6E767F";
/** 더 물러난 것 (축 눈금, 보조 숫자) */
export const FAINT = "#A3AAB3";
/** 테두리와 칸 사이 실선. 이 화면에서 가장 옅은 선이다 */
export const RULE = "#E4E7EB";
/** 채워진 막대 — 주(主) 계열 */
export const FILL = "#2A3138";
/** 채워진 막대 — 종(從) 계열. 주와 **밝기로만** 갈린다 */
export const FILL_WEAK = "#B7BDC4";
/** 막대의 빈 홈 */
export const TRACK = "#EAECEF";
/** 카드 바탕 — 흰 바탕 위에서 한 덩어리로 묶어 보이게 하는 아주 옅은 회색 */
export const SOFT = "#F5F6F8";
